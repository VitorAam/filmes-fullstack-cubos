import {
    Heading
} from '@chakra-ui/react';
import { Layout } from '../components/Layout';

export default function MovieDetailsPage() {

    return (
        <Layout>
            <Heading size="lg">Detalhes do filme</Heading>
        </Layout>
    );
}